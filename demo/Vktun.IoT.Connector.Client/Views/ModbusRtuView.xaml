<UserControl x:Class="Vktun.IoT.Connector.Client.Views.ModbusRtuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Padding="10">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Serial" Style="{StaticResource CardGroupBox}">
            <WrapPanel>
                <TextBlock Text="Port:" Style="{StaticResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding PortNames}" SelectedItem="{Binding SelectedPortName}" Style="{StaticResource InputComboBox}" Width="90"/>
                <TextBlock Text="Baud:" Style="{StaticResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding BaudRates}" SelectedItem="{Binding SelectedBaudRate}" Style="{StaticResource InputComboBox}" Width="90"/>
                <TextBlock Text="Data:" Style="{StaticResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding DataBitsList}" SelectedItem="{Binding SelectedDataBits}" Style="{StaticResource InputComboBox}" Width="60"/>
                <TextBlock Text="Parity:" Style="{StaticResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding ParityList}" SelectedItem="{Binding SelectedParity}" Style="{StaticResource InputComboBox}" Width="80"/>
                <TextBlock Text="Stop:" Style="{StaticResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding StopBitsList}" SelectedItem="{Binding SelectedStopBits}" Style="{StaticResource InputComboBox}" Width="90"/>
                <TextBlock Text="Slave:" Style="{StaticResource LabelTextBlock}"/>
                <TextBox Text="{Binding SlaveId, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}" Width="60"/>
                <Button Content="Open" Command="{Binding OpenCommand}" Style="{StaticResource PrimaryButton}" Width="80"/>
                <Button Content="Close" Command="{Binding CloseCommand}" Style="{StaticResource SecondaryButton}" Width="80"/>
            </WrapPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Read / Write / Poll" Style="{StaticResource CardGroupBox}" Margin="10,0">
            <WrapPanel>
                <TextBlock Text="Type:" Style="{StaticResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding RegisterTypes}" SelectedItem="{Binding SelectedRegisterType}" Style="{StaticResource InputComboBox}" Width="130"/>
                <TextBlock Text="Address:" Style="{StaticResource LabelTextBlock}"/>
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}" Width="80"/>
                <TextBlock Text="Qty:" Style="{StaticResource LabelTextBlock}"/>
                <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}" Width="60"/>
                <TextBlock Text="Data:" Style="{StaticResource LabelTextBlock}"/>
                <ComboBox ItemsSource="{Binding DataTypes}" SelectedItem="{Binding SelectedDataType}" Style="{StaticResource InputComboBox}" Width="100"/>
                <TextBlock Text="Value:" Style="{StaticResource LabelTextBlock}"/>
                <TextBox Text="{Binding WriteValue, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}" Width="130"/>
                <Button Content="Read" Command="{Binding ReadCommand}" Style="{StaticResource PrimaryButton}" Width="80"/>
                <Button Content="Write" Command="{Binding WriteCommand}" Style="{StaticResource SecondaryButton}" Width="80"/>
                <TextBlock Text="Interval:" Style="{StaticResource LabelTextBlock}"/>
                <TextBox Text="{Binding PollInterval, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBox}" Width="80"/>
                <Button Content="Start Poll" Command="{Binding StartPollingCommand}" Style="{StaticResource PrimaryButton}" Width="95"/>
                <Button Content="Stop Poll" Command="{Binding StopPollingCommand}" Style="{StaticResource SecondaryButton}" Width="90"/>
            </WrapPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Batch" Style="{StaticResource CardGroupBox}" Margin="10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <DataGrid Grid.Column="0" ItemsSource="{Binding TestDataList}"
                          Style="{StaticResource DataGridStyle}"
                          SelectedItem="{Binding SelectedTestData}"
                          Height="140">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="80"/>
                        <DataGridTextColumn Header="Data" Binding="{Binding DataType}" Width="90"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="120"/>
                        <DataGridTextColumn Header="Desc" Binding="{Binding Description}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0">
                    <Button Content="Add" Command="{Binding AddTestDataCommand}" Style="{StaticResource PrimaryButton}" Width="100"/>
                    <Button Content="Remove" Command="{Binding RemoveTestDataCommand}" Style="{StaticResource SecondaryButton}" Width="100"/>
                    <Button Content="Batch Read" Command="{Binding BatchReadCommand}" Style="{StaticResource PrimaryButton}" Width="100"/>
                    <Button Content="Batch Write" Command="{Binding BatchWriteCommand}" Style="{StaticResource SecondaryButton}" Width="100"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Results" Style="{StaticResource CardGroupBox}" Margin="10,0">
            <DataGrid ItemsSource="{Binding TestResults}" Style="{StaticResource DataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="80"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="120"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataType}" Width="80"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="70"/>
                    <DataGridTextColumn Header="Tx" Binding="{Binding Request}" Width="220"/>
                    <DataGridTextColumn Header="Rx" Binding="{Binding Response}" Width="220"/>
                    <DataGridTextColumn Header="ms" Binding="{Binding Duration}" Width="70"/>
                    <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" Width="80"/>
                    <DataGridTextColumn Header="Error" Binding="{Binding ErrorMessage}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="4" Header="Log" Style="{StaticResource CardGroupBox}" Margin="10,0,10,10">
            <TextBox Text="{Binding LogMessages}"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap"
                     FontFamily="Consolas"
                     FontSize="12"
                     Background="#F5F5F5"
                     Height="100"/>
        </GroupBox>
    </Grid>
</UserControl>
