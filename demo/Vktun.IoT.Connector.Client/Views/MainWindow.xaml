<Window x:Class="Vktun.IoT.Connector.Client.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="Vktun IoT Connector - 工业协议测试工具"
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="{DynamicResource HeaderBackgroundBrush}" Padding="10">
            <Grid>
                <TextBlock Text="Vktun IoT Connector - 工业协议测试工具"
                          FontSize="18" FontWeight="Bold"
                          Foreground="{DynamicResource HeaderTextBrush}"
                          VerticalAlignment="Center"/>
            </Grid>
        </Border>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Background="{DynamicResource NavigationBackgroundBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="协议类型"
                              FontSize="14" FontWeight="Bold"
                              Margin="10,10,10,5"
                              Foreground="{DynamicResource NavigationTextBrush}"/>
                    
                    <ListBox Grid.Row="1" 
                            ItemsSource="{Binding MenuItems}"
                            SelectedItem="{Binding SelectedMenuItem}"
                            Background="Transparent"
                            BorderThickness="0"
                            Margin="5">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <TextBlock Text="{Binding Icon}" 
                                             FontSize="16" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Title}" 
                                             FontSize="13" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
            
            <GridSplitter Grid.Column="1" Width="2" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Stretch"
                         Background="{DynamicResource SplitterBrush}"/>
            
            <Border Grid.Column="2" Background="{DynamicResource ContentBackgroundBrush}">
                <ContentControl prism:RegionManager.RegionName="ContentRegion"/>
            </Border>
        </Grid>
        
        <Border Grid.Row="2" Background="{DynamicResource StatusBarBackgroundBrush}" Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="5,0">
                    <Ellipse Width="10" Height="10" 
                           Fill="{Binding ConnectionStatusColor}"
                           Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding ConnectionStatusText}"
                             VerticalAlignment="Center"
                             Foreground="{DynamicResource StatusTextBrush}"/>
                </StackPanel>
                
                <TextBlock Grid.Column="1" 
                          Text="{Binding StatusMessage}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Foreground="{DynamicResource StatusTextBrush}"/>
                
                <TextBlock Grid.Column="2" 
                          Text="{Binding CurrentTime, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}"
                          VerticalAlignment="Center"
                          Margin="10,0"
                          Foreground="{DynamicResource StatusTextBrush}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
